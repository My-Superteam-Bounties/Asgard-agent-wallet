[signers]
# The local testing signer strategy
strategy = "SingleSigner"

[signers.local]
# This defines the fee payer wallet that the Kora node uses
type = "Local"
# For testing/devnet, we can point to a standard solana cli keypair
path = "~/.config/solana/id.json"
